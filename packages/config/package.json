{
  "name": "@monorepo/config",
  "version": "1.0.0",
  "description": "NOT listed in root workspaces - independent package that happens to live here",
  "dependencies": {
    "dotenv": "16.3.1"
  }
}
