[project]
name = "ml-pipeline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "scikit-learn>=1.3.0",
    "numpy>=1.26.0",
]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
