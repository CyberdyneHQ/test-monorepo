{
  "name": "m6-consumer",
  "version": "1.0.0",
  "private": true,
  "description": "Depends on nested workspace sibling m6-shared-lib (matched only via '**' glob). Should report shared-lib's 9 lodash CVEs as legitimate transitives.",
  "dependencies": {
    "m6-shared-lib": "*"
  }
}
